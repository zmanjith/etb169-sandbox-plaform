apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-sandbox-chart.fullname" . }}-test-connection"
  labels:
    {{- include "demo-sandbox-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-sandbox-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
